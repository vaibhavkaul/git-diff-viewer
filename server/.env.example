# Git Diff Viewer Server - Environment Variables

# REQUIRED: Path to directory containing git repositories
# This is where the server will look for git repos
SOURCE_DIR=/path/to/your/repos

# Optional: Server port (default: 3001)
# PORT=3001

# Optional: Ngrok URL for development
# When using ngrok to expose your local server, set this to your ngrok URL
# Both HTTP and HTTPS versions will be automatically allowed in CORS
# NGROK_URL=https://your-url-here.ngrok-free.app

# Example:
# SOURCE_DIR=/Users/username/code
# PORT=3001
# NGROK_URL=https://6c214c00a1a2.ngrok-free.app
