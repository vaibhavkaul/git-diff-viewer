# Git Diff Viewer Client - Environment Variables

# Optional: Client dev server port (default: 3000)
# VITE_PORT=3000

# Optional: Backend API URL (default: http://localhost:3001)
# Used by the API client to make requests to the backend
# VITE_API_URL=http://localhost:3001

# Optional: Backend API proxy target (default: http://localhost:3001)
# Used by Vite dev server to proxy /api requests
# VITE_API_TARGET=http://localhost:3001

# Optional: Ngrok URL for development
# When using ngrok to expose your local dev server, set this to your ngrok URL
# This allows Vite to accept requests from the ngrok domain
# VITE_NGROK_HOST=your-url-here.ngrok-free.app

# Example:
# VITE_PORT=3000
# VITE_API_URL=http://localhost:3001
# VITE_API_TARGET=http://localhost:3001
# VITE_NGROK_HOST=6c214c00a1a2.ngrok-free.app
